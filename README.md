# SmartLife-Agent
Plan your life and get the upper hand with SmartLife Agent

1. Run frontend
`npm run dev`

2. Run backend
`   uvicorn main:app --reload --port 8000`

Need to export "GEMINI_API_KEY" before running backend
`export GEMINI_API_KEY="Key here"`